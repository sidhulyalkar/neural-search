"""Overlay knowledge-graph schemas that index/link existing domain KG layers.

Modules here do not replace existing schemas (``neural_search.graph.schema``,
``neural_search.ontology.models``, etc.) — they define cross-cutting records
that bridge already-shipped vocabularies together.
"""

from __future__ import annotations
